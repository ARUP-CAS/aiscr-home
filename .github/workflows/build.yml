name: Build on Main

on:
  push:
    branches:
      - main
  # PR gate: build závisí na živém newsfeedu (aiscr-news), takže build + check
  # u PR odhalí regresi smlouvy s feedem už před mergem.
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '22'
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Type check
        run: pnpm check

      - name: Build project
        run: pnpm build

      - name: Create ZIP archive
        if: github.event_name == 'push'
        run: |
          cd build
          zip -r ../aiscr-home-latest.zip .
          cd ..

      - name: Upload build artifact
        if: github.event_name == 'push'
        uses: actions/upload-artifact@v6
        with:
          name: build-artifact
          path: aiscr-home-latest.zip
          retention-days: 90

